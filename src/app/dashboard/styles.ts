import { Box, styled } from "@mui/material";

export const ContainerDashboardStyles = styled(Box)(() => ({
  //height: '100vh',
  padding: "1rem",
  display: "flex",
  justifyContent: "center",
  alignItems: "center",
  width: "100%",
}));
